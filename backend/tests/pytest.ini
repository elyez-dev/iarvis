[pytest]
asyncio_mode = auto
testpaths = backend/tests
markers =
    unit: Unit tests (no external dependencies)
    e2e: End-to-end tests (require all services running)
    latency: Latency measurement tests
    accuracy: Accuracy/classification tests
